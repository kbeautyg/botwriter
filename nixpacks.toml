[phases.setup]
nixPkgs = ['python311', 'gcc']

[phases.install]
cmds = [
  'python -m venv /opt/venv',
  '/opt/venv/bin/pip install --upgrade pip',
  '/opt/venv/bin/pip install -r requirements.txt',
]

[start]
cmd = '/opt/venv/bin/python -m post_bot'

[variables]
PYTHONUNBUFFERED = '1'
